# CLAUDE.md — 작업 지침

## 작업 원칙 (모든 작업에 우선 적용 — 사장님 지시 2026-08-21)

1. **해결책을 끝까지 찾는다.** 한 방법이 막히면(라이브러리 오류·권한·경로·네트워크) 즉시 대안을
   스스로 찾아 시도한다: 다른 라이브러리, 우회 경로, 순수 HTTP(requests) 직접 호출, 다른 도구·인증.
   "안 됩니다"로 끝내지 않고 되는 길을 만들어 온다.
2. **설명보다 실행.** 길게 설명만 하다가 나중에 찾아보지 않는다. 먼저 시도해서 되는 방법을 찾고,
   실행한 뒤 결과로 말한다.
3. **정말 안 될 때만, 정확한 근거로 보고.** 여러 방법을 다 시도했는데도 안 되면, 그때 "무엇을 어떤
   방법들로 시도했고 각각 왜 안 됐는지"를 논리적 근거와 사실로 댄다.
4. **미루기는 최후.** 막히면 사장님께 넘기기 전에 대안을 먼저 다 써 본다.

## 계정 시스템 맵

- **인스타/스레드 카드뉴스 봇**: 이 저장소(bighitcho/bighit). GitHub Actions 하루 3회 자동.
  현재 `GEMINI_API_KEY` 등 Secrets 미등록으로 실행 전부 실패 중 → 키 등록해야 가동.
- **블로그 자동발행(@알파남@)**: 구글드라이브 `내 드라이브/@알파남@/블로그수익화/블로거-자동포스팅`.
  블로그스팟 12개 + 워드프레스 3개. 데스크톱 예약작업(08:30 발행 / 22:30 검증).
  클라우드에서도 드라이브의 `token.json` 으로 Blogger REST API 직접 조작 가능
  (google 라이브러리의 cryptography 가 이 환경에서 깨지므로 순수 `requests` 로 호출).
- 작업 원칙 원본: 드라이브 `내 드라이브/00_작업원칙_해결우선.md`, 허브 `00_클로드_작업허브.md`.

## 애드센스 개선 진행 (2026-08-21)

golden628 "가치가 별로 없는 콘텐츠" 거절 대응:
- `_공통.md` → V3 (AI 티 제거·정직한 경험 서술·매 글 구조 다양화·GEO/AEO·썸네일 다양화 규칙)
- 발행량 69편/일 → 14편/일 (양산 신호 제거)
- 신뢰 페이지(개인정보처리방침/소개/문의) 블로그스팟 12개에 생성
- 남은 것: 썸네일 매일 다양화 코드, preflight AI 티 검사 강화, 테마 푸터 링크, 색인 회복 후 재심사
